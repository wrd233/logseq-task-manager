# V2 设计到代码追踪矩阵

> 初版日期：2026-07-19。状态只表示 V2；V1 自动测试只能登记为 `REUSE_EVIDENCE`，不能把 V2 条目标成完成。

## 状态

- `NOT_STARTED`：V2 尚无实现；
- `REUSE_EVIDENCE`：V1 有可复用代码/测试，但 V2 契约或架构尚未满足；
- `AUTOMATED_FOUNDATION`：V2 代码、自动成功/失败路径和文档已建立，但 Desktop 或真实进程 Gate 尚未完成；
- `DESKTOP_PARTIAL_PASS`：已有真实 Desktop 与持久化证据，但该需求的其他交互、失败路径或视觉 Gate 仍未全部完成；
- `RUNTIME_BLOCKED_BY_CONFIG`：自动骨架可继续，最终 Gate 依赖用户提供真实配置；
- `DONE`：代码、失败路径、文档和对应 Gate 证据全部通过。

## 横切要求

| requirement_id | 文档章节 / 决定 | 用户场景 | Domain 规则 / 不变量 | Application 用例 | Adapter / UI | 测试 | Slice | 状态 | 当前证据 / 缺口 |
|---|---|---|---|---|---|---|---|---|---|
| V2-ARCH-001 | D-180..191；V2 §5.4-5.5 | Plugin、CLI、Agent 看到同一事实 | 正文/状态/历史/投影单一权威 | 所有 command/query 共享一层 | Service client；SQLite adapter | boundary + integration | A | DONE | V2 Application/Repository 原子 command seam、CLI 与 Plugin 均经 Local Service；正常 Plugin 入口不再 import/初始化 V1 Application、FileStorage 写 Runtime 或暴露 V1 Inbox/Audit 写按钮，未知 action fail closed。Now Work、Object、Review、Project 重入、Migration、Audit 均读同一 V2 Service 投影；Project 重入组合 Object/Ownership/Association/Now Work，Audit 区分查询失败与空历史。Desktop 实测 V2-only 导航、READY 连接、Service Pending/Conflict 事实与只读恢复指引；证据见 `docs/runtime/V2_RUNTIME_V1_WRITE_UI_RETIREMENT_DESKTOP_REPORT.md`。 |
| V2-ARCH-002 | D-216；V2 §30-32 | Service 不可用仍可写正文 | Domain 无 transport 语义 | health/status/restricted | 单个 loopback Service | unavailable/protocol | A | DONE | descriptor 0600、auth、timeout/unavailable/protocol restricted 自动测试通过。Desktop 又真实验收 descriptor 缺失、loopback 无监听进程的 `SERVICE_UNAVAILABLE`、v2 descriptor 的 `SERVICE_PROTOCOL_MISMATCH` 及 reload 持久；同一普通 Block UUID 在受限态下可编辑与读回，Store `NOT_STARTED`、formal writes false。只复用 session-only reconciliation flag 与有界 warning，没有持久队列、第二状态源或新写路径。P0-H 又在不改变正式写入路径的前提下加入 ADR-0008 独立 Launcher：真实 LaunchAgent、Graph-bound lease、最后租约/TTL owned shutdown、Service/Launcher crash recovery 与 orphan self-stop 均通过，同一 SQLite 保持对象；Desktop lifecycle Gate 单独保留在交互验收报告。证据见 `docs/runtime/V2_FIRST_RUN_RESTRICTED_DESKTOP_REPORT.md` 与 `docs/implementation/task-copilot-v2-ux/logs/p0-h-launcher-lifecycle-runtime-20260724.md`。 |
| V2-DATA-001 | D-190..192；V2 §33-34 | 每 Graph 独立运行和恢复 | SQLite 唯一当前状态；schema 独立版本 | initialize/migrate/backup | SQLite + `.task-copilot/` | idempotency/locked/corrupt | A | DONE | Graph-bound schema v12；显式快照后 v1..v11→v12 受控升级。v12 仅在 `objects` 增加受 object_type/JSON 约束的 Project aggregate，迁移保留 Object/Anchor 并为旧 Project 建立可重入默认值；未新增 Project 子表或状态源。既有 Commit/Proposal/Audit/Candidate/迁移能力及失败全量回滚、Doctor、Backup/Restore 证据保持。 |
| V2-DOM-001 | D-214、D-220；V2 §13-22 | 六类对象、三层状态可理解 | 六类封顶；Lifecycle/Condition/Focus 封顶 | create/change/focus | UI 弱提示 | domain matrix | A | DONE | V2 pure Domain contracts 与 Application create/lifecycle/change_condition/undo_lifecycle tests 通过；Condition 输入封闭且版本/幂等/Audit 单事务，不改变 Lifecycle/Focus。Area 已走完受控创建、编辑和 stale 零覆盖。原因化取消/重开只经唯一 Proposal 与既有单步 Domain SemanticCommit，inverse Commit 受对象版本保护。Desktop 已验证 Task MEDIUM 取消/重开/reload/Undo、Project HIGH 取消/reload/Undo，以及 MiniProject HIGH 空原因拒绝、取消、显式重开、两次 reload 与重开 Undo；三类对象均保持正文、Anchor、Condition、Focus 与 Ownership 分离。 |
| V2-PROJECT-001 | D-049..071；V2 §34.7 | Project 一屏看到目标、交付、阶段与当前推进 | Project-owned bounded aggregate；1–3 focus；阶段不移动对象；无百分比 | propose/review/update/undo | Object workspace + reentry | domain + migration + service + UI + Desktop | B,C,E | DONE | OD-006 已由 `0006-project-structure-aggregate.md` 关闭。Domain Validator、schema v11→v12 备份迁移、Application command、唯一 HIGH Proposal、版本重验、单步 Domain SemanticCommit、幂等 Commit、inverse Commit 与后续版本零覆盖均已自动通过。隔离 Logseq Desktop 又真实完成编辑→HIGH Review→Commit→renderer reload v3 读回→专用 Undo→再次 reload v4 精确恢复；Graph、位置、归属不变，Pending/Recovery 0。reload 暴露的旧长轮询接管冲突已在既有 Controller 内以有界重试修复，同一 Service 无需重启即恢复 READY；证据见 `docs/runtime/V2_PROJECT_STRUCTURE_DESKTOP_REPORT.md`。 |
| V2-ID-001 | D-030..041；V2 §21 | 移动/改名不丢身份 | object_id 独立；最多一 Primary Anchor | bind/observe/rebind | Graph UUID adapter | uniqueness/move/delete | A-B | DONE | 初始绑定、观察、move/copy 与 rebind 合同已通过；Desktop 原生跨页移动在 reload 后保持同 object_id/active anchor_id/Block UUID/版本/Ownership/Association，去 properties 复制得到独立 UUID/object_id/anchor_id。真实删除又证明对象保留、旧 Anchor 变 `missing`；新同类型 Block 在预览、独立确认和 stale 重读后持久复核 `id:: <UUID>`，再经唯一 Service rebind。缺少确认零写入；确认后旧 `replaced`、新唯一 `active`，object_id/Ownership 不变，Audit/Receipt 各一次，cold reload 保持。仍是原已知 Anchor reconciliation 与原 rebind 事务，未新增扫描器或恢复器；证据见 `docs/runtime/V2_SLICE_A_B_DESKTOP_REPORT.md`。 |
| V2-OWN-001 | D-035..047；V2 §21 | 位置变化不改归属 | 最多一个 Primary Owner；Association 不冒充归属 | change ownership/add association | 对象页显式影响与确认 | matrix/cycle/scope | A-C | DONE | Primary Ownership 由唯一表/事务维护，与 Association 和位置明确分开。显式 change 只接受唯一 HIGH Proposal，Service 权威重读 child/new Owner/current Owner，并用单步 DOMAIN_WRITE SemanticCommit；专用 inverse Commit 可恢复旧 Owner，后续变化零写入，receipt 后中断可幂等续完。普通 Association 与 Primary Ownership Desktop 已通过独立确认、Commit、reload、Undo。E2E-04 又以 Logseq 原生 Cut/Paste 跨页移动证明同一 object_id、active anchor_id、Block UUID、object version、Ownership 与 Association 在移动前后及 reload 后完全不变；E2E-05 复制则产生独立 Object/Anchor 且不继承归属。 |
| V2-PROP-001 | D-094..122；V2 §23-28 | 可读、可拆分、可过期的建议 | Proposal 非事实；read/modify scope；group dependency | validate/revise/review/revalidate | 两文件、文本/语义 Diff | schema/stale/partial | C | DONE | V2 runtime Schema、两文件、scope/hash/risk/dependency、schema v4 持久化、submit/list/get/review 和 Review UI 已通过。Desktop 已证明接受不生效、拒绝、独立组部分接受、真实 Provider 原位调整、stale、Commit/Undo 与 UC-28 双 Proposal 隔离。2026-07-22 真实两组 Gate 又证明依赖后置组不能先接受，前置组暂缓时间/原因可见并跨 reload 恢复，仍受依赖保护；按顺序接受只进入 `ACCEPTED`，正式状态零写入。C3 继续重读 read scope 与 accepted modify target，最终确认保持独立。 |
| V2-COMMIT-001 | D-185..188；V2 §29 | 提交失败不假成功，可 Undo | version/hash；Partial Failure；Undo 不覆盖新编辑 | commit/undo/recover | Graph + SQLite step ledger | fault injection/restart | C | DONE | C4/C5 已形成 PENDING-first Commit、Graph before/after 重读、Application Object/Anchor/Audit/Receipt 物化、inverse Commit、精确当前投影删除、正向 `UNDONE`、幂等重试和 PENDING/RECOVERY_REQUIRED 重启续跑。Desktop 已通过正常 Commit/Undo、后续编辑零覆盖、原因化 Lifecycle Undo、UC-28 写后旧读保护。2026-07-22 正向与逆向流程都在 `PREPARED/PENDING` 后真实终止 Service；Desktop 明确 restrict，重启后复用原 ledger 完成，四步 VERIFIED、正向 UNDONE、逆向 COMPLETED、正文恢复、测试投影移除且 Pending/Recovery 0。 |
| V2-LLM-001 | D-125..144；V2 §40-46 | 局部语义产生 Proposal | Provider/Model 不进 Domain；非法输出零写入 | analyze/scan/revise | Provider/Prompt/Profile runtime | mock/live/golden/security | D | DONE | Keychain reference、Flash/Pro actual model、严格无答案 Prompt、Schema/Validator、零写入与 secret scan 已证明；Flash/Pro 各完成固定 22/22 L3 `PIPELINE_PASS`。L4 Desktop 又完成当前块 loading/error、READY/NO_PROPOSAL、直接接受、拒绝、同 proposal_id 调整后接受和两组部分接受；模型不能改变 Block scope/group/operation/target，意图漂移保留原 Proposal。运行结束前 Object/Commit 为 0，证据见 `docs/runtime/V2_DEEPSEEK_PROVIDER_L4_DESKTOP_REPORT.md`。 |
| V2-VIEW-001 | D-145..171；V2 §9-10、§57-59 | Now Work 与审阅中心低噪可扫读 | Projection 可重建；无 AI 分数 | query now/review | 三区域；两视图；键盘/主题 | VM/UI/Desktop | E | DONE | Now Work 三个空隐藏区域、Focus/Condition/期限/Anchor 打开与 Task/Project/Area 筛选分组均有真实 Desktop 证据；局部视图不暴露排序，筛选前后 Focus rank、对象版本和 Audit 不变。Review Center 的 Candidate 原文优先/四处置/CREATE/UPDATE、Proposal 文本与语义 Diff、部分接受、高影响确认、Commit/Undo 及双空态均已实测。`c9919f2` 又以真实 Attention session 证明一对象一主问题、取消不误记 acted、session-only 处置、reload 重算和事实解除自动失效，不新增提醒权威。Enter/Space 可操作主导航与审阅切换，重绘后稳定恢复同一控件焦点；ARIA current/pressed 与焦点环明确。真实 renderer 的 Light/Dark 媒体条件分别命中独立 token。无新持久状态或第二主题权威。证据见 `docs/runtime/V2_NOW_WORK_PROJECT_AREA_FILTER_DESKTOP_REPORT.md`、`docs/runtime/V2_VIEW_KEYBOARD_THEME_DESKTOP_REPORT.md` 与 `docs/implementation/task-copilot-v2-ux/logs/p1-attention-quality-boundary-desktop-live-20260730.md`。 |
| V2-CLI-001 | D-128..135；V2 §36-39 | Agent 确定性获取有限上下文 | CLI 无领域逻辑、无 force apply | object/graph/context/proposal/skill/doctor | Service API + CLI | help/json/exit/integration | A,F | DONE | 可执行 status/doctor/object/graph page/block/resolve、Proposal list/show/validate/submit、Skill list/show 与四种 Context export，JSON schema、退出码和真实 Service 集成通过。真实 Logseq 0.10.15 已补齐 Page/Block entity shape、父链 Page 边界、wrapped resolve、Context Package、reload 后 UUID 变化与 bridge Doctor；Graph 只读不读文件、不缓存正文。真实 DeepSeek 外部 Agent Proposal 经 CLI validate/submit 后进入同一 Plugin Review，接受仍保持 Object/Commit 为 0，CLI 没有 apply/commit。证据见 `docs/runtime/V2_E2E13_GRAPH_CONTEXT_DESKTOP_REPORT.md`。 |
| V2-MIG-001 | D-193..202；V2 §51 | 用户小批次迁移且可撤销 | 手动、幂等、部分采用合法 | scan/preview/commit/undo batch | Settings/CLI | interruption/repeat/rollback | F | DONE | 全部旧 Phase/Condition/Signal 的确定性可审阅映射与校验 Recovery Bundle Scan 已通过；Application/SQLite 支持 ≤50 项原子 Object/Anchor/Ownership 导入、同内容幂等、Verify、变化保护 Undo、Undo 后重试、重启续读和单一 Activate。真实 V1 Pilot 后 bundle 已在 Desktop 完成 8 项 Scan/完整 Decisions、Backup、确认保护、Import 重放、IMPORTING Service 重启/show、Verify、单批 Undo、新键重试与 Activate。首轮当前 Graph 缺失 Anchor 经既有 reconciliation 改变投影后使 Verify 安全拒绝且未激活；最终只导入当前 Anchor 可解析范围，Run ACTIVATED、Doctor 12 PASS/0 WARN。Plugin 只读工作区显示 PREVIEWED/IMPORTING/VERIFIED/ACTIVATED、计数、恢复点与下一步，editable inputs=0，不接收 Bundle 或执行直接写入。迁移账本不复制当前正文且不复用 SemanticCommit；证据见 `docs/runtime/V2_MIGRATION_COPIED_DATA_DESKTOP_REPORT.md`。 |
| V2-OPS-001 | D-192、D-203..204；V2 §53-56 | 故障可诊断、备份可恢复、Key 安全 | 高影响修复走 Proposal；secret 永不记录 | backup/restore/doctor/diagnostics | Settings/CLI | restore/redaction | A,F | DONE | 受控 Service Backup API、0700/0600、防覆盖、Graph/schema/integrity/foreign-key 只读校验、路径隔离和错误脱敏已自动证明。结构化 Doctor、0600 diagnostics.zip 与真实 `unzip -t` PASS 保持；L4 Desktop 已实测 Graph bridge、真实 Provider、默认 ready/no-proposal/revised 日志、两次 Diagnostics JSONL 导出和 credential-looking pattern 零匹配。Restore Desktop 完成固定确认、恢复点、原子激活、descriptor 删除、Service stop、Plugin restricted、同库重启和恢复后 Doctor；首次真实失败还收敛了陈旧 READY 与 bridge 日志风暴，未增加状态源或恢复器。 |
| V2-FIRST-001 | D-197..198；V2 §32.1、§33 | 首次启用时空系统可理解、可选择下一步 | 首次启动不扫描、不迁移、不调用模型 | initialize/check graph/status | 非敏感配置模板；欢迎页仅含开始使用、迁移现有内容、检查系统状态；失败进入受限模式 | first-run/reload/Desktop | A | DONE | 自动测试与 Desktop 完整证明三入口、空 descriptor 在 Adapter/V1 领域 FileStorage 前停止、Store `NOT_STARTED`、formal writes false；欢迎页 editable controls 为 0，没有自动 Graph 扫描、Recovery Bundle 读取、迁移或 Provider 调用。协议错误及 reload 后正文仍可编辑，清理后测试页与私有 descriptor 均不存在；证据见 `docs/runtime/V2_FIRST_RUN_RESTRICTED_DESKTOP_REPORT.md`。 |

## E2E-01..24

| requirement_id | 文档章节 | 用户场景 | Domain 规则 / 不变量 | Application 用例 | Adapter / UI | 自动 / 运行测试 | Slice | 状态 | 当前证据 / 缺口 |
|---|---|---|---|---|---|---|---|---|---|
| E2E-01 | V2 §68 | 写 `[任务]`，重启、改标题、Now Work 可见 | Task OPEN；ID/Anchor 稳定 | materialize/sync/query | block event/parser/Now | fixture + Desktop | B,E | DONE | `explicit-sync-controller.ts` 与 `v2-explicit-candidate-discovery.ts` 复用同一 Service/Proposal 边界。Desktop 已证明真实物化、同 object_id/anchor_id 标题更新、Now Work/reload，以及当前页 Candidate 整批保存 Object=0、非法项计数、正文变化后同步旁路拒绝、旧视图 stale 零写入、重扫后只处理一项的 Proposal/Commit/Undo。无 Anchor 且已有未完成 Candidate 时，Service 返回 `V2_EXPLICIT_CANDIDATE_REVIEW_REQUIRED`，不会绕过审阅直接物化；已有 Anchor 同步不受影响。证据见 `docs/runtime/V2_CANDIDATE_REVIEW_DESKTOP_REPORT.md` 与 `docs/testing/v2-candidate-multi-stale-desktop-2026-07-22.json`。 |
| E2E-02 | V2 §68 | `[任务] 标题` 无 Marker | Marker 不决定身份 | materialize | parser | unit + Desktop | B | DONE | 有/无 Marker Parser、Plugin 事件交付与无 Marker 首次物化路径已自动证明。Desktop 已证明 Task DONE、重复 DONE、移除 Marker 不重开、终态相反 Marker 零写入，以及 CANCELED 在记录原因前零写入；TODO/NOW/DOING/WAITING 不决定身份、Condition 或 Focus。MiniProject 三问已达 Marker/侧栏/外部 Agent 自动合同，统一进入唯一 HIGH Proposal，接受不生效，最终确认后走单一 Domain SemanticCommit。object-only 已在 Desktop 通过侧栏发起、三问、ACCEPTED 零正式写入、最终 Commit、Anchor 不变、reload 及 Backup/Doctor。Marker/DONE 也已用真实 `DONE [MiniProject]` Block 事件通过：Proposal 绑定 Block hash + active Anchor + Object version，ACCEPTED 仍 OPEN，三重验后才 COMPLETED，正文 DONE 与 Anchor 证据保留，reload/Backup/Doctor PASS。运行中修复了旧 Parser 只识别 `[MiniProject] DONE` 而不识别 Logseq 真实前置 Marker 的问题，并新增内外 Marker 冲突拒绝。证据见 `docs/implementation/V2_MINI_PROJECT_CLOSURE_CONTRACT.md`、`docs/runtime/V2_MINI_PROJECT_CLOSURE_DESKTOP_REPORT.md` 和 `docs/runtime/V2_MARKER_DESKTOP_REPORT.md`。 |
| E2E-03 | V2 §68 | Task 下裸 TODO 保持内部步骤 | 内部 TODO 无 object_id | synchronize block | subtree parser | fixture + Desktop | B | DONE | 自动合同覆盖 300ms 重读、32 根队列、256 Block 预算、已验证前缀、异常停写与卸载 cancellation。Desktop 0.10.15 已真实验证逐项编辑、快速连续编辑、整树批量粘贴和 Service 中断恢复：2 Task + 2 Decision 各有独立 Object/active Anchor，两个裸 TODO 均无 `id::`/无 Object，快速编辑只交付最新值。真实首次身份回声误抑制缺陷已以 `c28fa06` 修复；33 根溢出显式 reconciliation，257 子块 bridge 失败保持零额外写入，cold reload 后 pending 0、reconciliation false、Doctor PASS。事件入口集成测试进一步以 257 节点精确证明只触达前 256 项并发出 `EXPLICIT_SYNC_SUBTREE_TRUNCATED`；进行中 unregister/dispose 不再读取后续节点或迟到写入，A-RT-03.18 工程边界已收口。证据见 `docs/runtime/V2_EXPLICIT_SUBTREE_DESKTOP_REPORT.md`。 |
| E2E-04 | V2 §68 | 跨页移动 | ID/Ownership 不变，Anchor 更新 | observe/sync | UUID event | integration + Desktop | B | DONE | 自动 fixture 与 Desktop 0.10.15 均通过：Logseq 原生 Cut/Paste 跨页移动后，同 UUID 经统一 Service 保持 object_id/active anchor_id、版本、Primary Ownership 和普通 Association；冷 reload 后读回一致，源页不再含该 Block。 |
| E2E-05 | V2 §68 | 复制正式 Block | 新 UUID 不继承 ID | detect copy/candidate | Graph adapter | fixture + Desktop | B | DONE | 自动 fixture 与 Desktop 0.10.15 均通过：去 properties 复制正式显式 Task 得到新 Block UUID、新 object_id 与新 active anchor_id；原对象保持不变，复制对象不继承 Ownership/Association，冷 reload 后两者均 active。真实事件证明 `event.blocks` 已足够，未保留会误收瞬态空 Block 的 `txData` 平行补漏。 |
| E2E-06 | V2 §68 | 删除 Primary Anchor | 对象保留、Anchor Conflict | consistency check/rebind | Graph adapter/review | fake + Desktop | B,E | DONE | Desktop 0.10.15 已走完真实删除→cold restart→已知 UUID `missing`→同类型未绑定 Block 预览→缺少独立确认零写入→确认 rebind→再次 cold reload。原 Object 始终保留且 object_id 不变，version 2→3→4；旧 Anchor `missing`→`replaced`，新 Block 先持久并复核 `id:: <UUID>` 后成为唯一 `active` Anchor。未确认前 version/Anchor/Audit 计数全不变；确认后只增加一条 rebind Audit/Receipt，reload 后 Doctor PASS/Graph bridge connected；证据见 `docs/testing/v2-anchor-delete-rebind-desktop-2026-07-22.json`。 |
| E2E-07 | V2 §68 | 分析普通 Block | 模型只建议 | analyze block | LLM/Review | mock + live + Desktop | D | DONE | 严格 L3 通过；真实 Desktop 当前块 DS-02 返回可读 `NO_PROPOSAL` 理由且零持久化，有效 Journal 进入 READY Proposal/Diff。loading 和 Validator error 均可见且恢复操作，任何模型结果都没有直接写 Graph 或正式 Store。 |
| E2E-08 | V2 §68 | 接受表达、拒绝升级 | group dependency 合法 | review/commit/revalidate | Review Center | property + Desktop | C | DONE | 自动 group dependency、部分接受、高影响确认、拒绝升级、accepted-only Commit 均通过。Desktop 已真实显示上下文/最终预览/文本与语义 Diff，完成 MEDIUM 接受、拒绝、独立组部分接受及高影响专项确认。2026-07-22 两组依赖 Gate 证明后置组先接受明确失败，前置组可暂缓并跨 reload 保留时间/原因，按顺序接受后只显示 accepted-not-applied 与最终 Commit 入口。 |
| E2E-09 | V2 §68 | 原文变化后提交旧 Proposal | UUID/hash/version 不匹配停写 | validate/commit | Graph + SQLite | conflict integration | C | DONE | V2 C3/C4 在 prepare 与 Graph 写前后重读 UUID/hash/version；真实 Desktop 接受 Proposal 后修改原文，再确认最终 Commit，Proposal 变为 `STALE`，原文保持后续内容，对应 Object/Anchor/Commit 均为 0；没有静默覆盖。 |
| E2E-10 | V2 §68 | Undo 正文和 Store，不覆盖后续编辑 | Undo 新 Commit | undo/recover | Graph + SQLite | fault + Desktop | C | DONE | 自动 inverse Commit、重启续跑、后续编辑零覆盖通过。Desktop 已真实证明 Commit 后修改正文时 Undo 明确零写入；正常 Formalization、Lifecycle 与 UC-28 独立遗留 Undo 均通过。2026-07-22 逆向 Commit 在两步 PREPARED 后真实终止 Service，重启后从同一 Review 操作续完；正文恢复并保留稳定身份，Object/Anchor 当前投影精确移除，正向 UNDONE、逆向 COMPLETED、reload 后历史可见且不重复提供 Undo。 |
| E2E-11 | V2 §68 | Focus/期限/阻碍/Waiting 可解释排序 | Focus 非状态；Waiting 安静；无分数 | query now work | Now Work UI | VM + Desktop | E | DONE | 自动成功/失败合同保留。Desktop 已真实点击 Focus 加入、两项上移排序、WAITING/BLOCKED 表单、Task 本地时间期限、可读 blocker 选择、阻碍对象唤醒、Task 筛选/按类型分组和 Primary Anchor 打开；CLI 读回对象 version 9 及 Condition/dueAt/blockerObjectId，冷重启后 Focus 顺序/期限/状态保持，无分数。future-review Waiting 不再误报到期，已在 Focus 中的等待项不再显示重复加入操作。证据见 `docs/runtime/V2_SLICE_A_B_DESKTOP_REPORT.md` |
| E2E-12 | V2 §68 | 审阅中心原文优先、四处置 | Candidate/Proposal 分离 | disposition/review | two-tab Review | UI + Desktop | E | DONE | SQLite v10 持久化单一 Candidate authority，正文只在 Logseq；Plugin 对显示队列和来源瞬态重读使用同一个 50 项上限，并按原文→理由→建议呈现。同版本记忆处置，普通拒绝随来源变化重开，`NO_MORE_LIKE_THIS` 仅在稳定来源+类别+建议相同且正文普通编辑时保持，不会压制不同目标建议；已有当前 Proposal 时以 409 拒绝平行处置。Service 集成与 Desktop 均已真实走完 CREATE 扫描、四处置、唯一 Proposal、READY/ACCEPTED 零正式写、Commit resolved、Undo reopen 与 reload。UPDATE 也已在 Desktop 走完目标选择、编辑完整最终正文、红/绿文本 Diff、单组 `REWRITE_BLOCK`、accepted-not-applied、同 object_id 版本 4→5 Commit、reload、逆向 Undo 版本 5→6、再 reload 与 Candidate reopen。真实运行发现并修复 Logseq `id::` 持久化的 identity-only 假 stale：复用 Candidate rediscovery 仅刷新未改正文版本，正文 hash 变化仍零写入拒绝。Pending/Recovery Commit=0、SQLite integrity/FK 通过，无新表、状态、Commit 类型或恢复器；合同见 `docs/implementation/V2_CANDIDATE_UPDATE_CONTRACT.md`，Desktop 证据见 `docs/runtime/V2_CANDIDATE_REVIEW_DESKTOP_REPORT.md`。 |
| E2E-13 | V2 §68 | 外部 Agent 导出、验证、提交、插件审阅 | submit != commit；scope 封闭 | export/validate/submit | CLI/Review | CLI integration + Desktop | F | DONE | 真实 Desktop 已连续走完 Page/Block/resolve bridge、四范围中的 Page Context Package、DeepSeek v4 外部 Agent、CLI validate/submit 和 Plugin Review。首轮 Structured JSON 被 Domain Validator 拒绝且零写入；完整字段模板后生成唯一 MEDIUM Proposal。validate 保持 0/0/0，submit 为 Object 0 / Proposal 1 / Commit 0，Plugin 显示 EXTERNAL_AGENT/模型/Diff 并接受后仍无正式写入；最终确认独立保留。证据见 `docs/runtime/V2_E2E13_GRAPH_CONTEXT_DESKTOP_REPORT.md` 与脱敏交互 JSON。 |
| E2E-14 | V2 §68 | 手动小范围迁移、预览、幂等、Undo | migration batch 可恢复 | scan/commit/undo | Settings/CLI | fixture + Desktop | F | DONE | 真实 copied-data bundle 已走完只读 Scan、完整人工 Decisions、Preview、Backup、缺确认零请求、小批 Import、重放幂等、IMPORTING Service restart/show、Verify、单批 Undo、新键重试和显式 Activate。当前 Graph 已失效的 Anchor 使首轮 Verify 安全拒绝，最终审阅范围只包含当前可解析 Anchor；源文件 SHA 不变。Plugin 迁移卡片四态与激活后对象读回通过，最终 batch1 UNDONE、batch2 VERIFIED、Run ACTIVATED、对象/active Anchor 各 1、Doctor PASS。证据见 `docs/runtime/V2_MIGRATION_COPIED_DATA_DESKTOP_REPORT.md` 与脱敏 JSON。 |
| E2E-15 | V2 §68 | Service 故障时正文可编辑 | 无 Service 不允许正式写入 | health/recover/check | Plugin restricted | process fault + Desktop | A | DONE | 自动合同已覆盖有界会话队列、溢出显式 reconciliation 与最新值重连；2026-07-20 Desktop 又证明真实 Service 停止后正文连续保存，重连后 SQLite 同一对象只接收 latest 值。2026-07-22 新 Gate 在无 Service 与协议不兼容两种受限态及 Logseq reload 前后，用同一 UUID 读回并再次编辑普通正文；Store 一直 `NOT_STARTED`、formal Object 为 0、pending 为 0、formal writes false。受限期间只有有界 warning 和 session-only recovery state，没有持久队列或第二写路径；证据见 `docs/runtime/V2_FIRST_RUN_RESTRICTED_DESKTOP_REPORT.md`。 |
| E2E-16 | V2 §68 | Graph 成功、Store 失败 | 明确 Partial Failure | commit/recover | Graph + SQLite | injected failure + Desktop | C | DONE | V2 自动测试已覆盖 Service materialization conflict→`COMPENSATION_REQUIRED`、Doctor 暂时 FAIL、Graph 补偿后 Commit/Proposal FAILED 与 Doctor 恢复。Desktop 0.10.15 又在隔离库用既有同步入口建立 active Anchor 冲突：提交前版本保护先安全拒绝一次；稳定身份重试真实写入 Graph 后 Domain 失败，Plugin 明示失败、恢复原正文且不报成功。最终 Proposal/Commit 为 `FAILED / DOMAIN_WRITE_FAILED`，Graph step `COMPENSATED`、Domain step `PREPARED`，唯一原对象不变，reload 后终态保持，Pending/Recovery 0、Doctor PASS；没有 fault API、直接 SQLite 写入或第二恢复路径。证据见 `docs/runtime/V2_E2E16_DOMAIN_FAILURE_COMPENSATION_DESKTOP_REPORT.md`。 |
| E2E-17 | V2 §68 | 恢复快照后 Doctor PASS | 恢复后必须一致性检查 | backup/restore/doctor | SQLite/CLI | temp restore + Desktop | A,F | DONE | 真实 Desktop 隔离 Task v2 快照后改为 v3；缺确认退出码 2 且零请求，正确 Restore 创建含 v3/期限的恢复点并原子激活 v2，删除 descriptor 后 Service 在 Desktop 仍运行时退出。Plugin 自动显示 READ_ONLY_SAFE_MODE/RESTRICTED/formal writes false 且只记录一次 transport failure；同库重启后 Plugin/CLI 读回 v2、两个 Backup validate PASS、Graph bridge 与 Doctor 12 PASS/0 WARN。证据见 `docs/runtime/V2_SQLITE_RESTORE_DESKTOP_REPORT.md` 与脱敏 JSON。 |
| E2E-18 | V2 §68 | Key 不出现在任何默认资产 | secret 非领域数据、永不记录 | config/log/export | Provider/diagnostics | secret canary scan | D,F | DONE | Provider 只接受 env/Keychain reference；真实运行后 Git/ignored Graph 原文 canary 无匹配。L4 Desktop 默认结构化日志只记录关联 ID、Block UUID、Proposal ID 和结果；两次实际导出的 JSONL 与提交报告经 credential-looking pattern 扫描为 0。 |
| E2E-19 | V2 §68 | Project 对象和页面原子创建 | Project 必须页面；不半成功 | prepare/finalize project | Projects UI + exact Page Adapter + SQLite ledger | integration/fault injection + Desktop | B | DONE | 自动合同覆盖 prepare/finalize、未知同名/意图不匹配零写入、幂等、改名和事务 fault injection。Desktop 已真实创建 Project 并在 V2-only 对象列表显示 Lifecycle/Condition；未知同名页 UUID/属性不变且零对象新增；受控页改名与冷 reload 保持 UUID/object_id。真实拦截 finalize 并停止 Service 后 descriptor 清理、SQLite 目标对象为 0、受控页保留；重启同意图复用预发行身份并只收口一个 Project。复用现有 SemanticCommit step ledger，无专用表/双写/扫描；证据见 Desktop 报告。 |
| E2E-20 | V2 §68 | Closure 可说明未完成目标 | Completion 不要求全部 Objective | close project Proposal | Agent/Review | fixture + Desktop | F | DONE | 外部 Agent Proposal 用一个 HIGH 组绑定结构化 Closure 与 COMPLETED；Validator 强制原始目标/结果/Deliverable/未完成原因与后续/遗留/Decision/未来总结，但不强迫所有 Objective 完成。固定 fixture 与真实独立 Agent 输出均经真实 CLI 文件 `validate → submit → Local Service`进入 Review queue；首次 Agent 错误 Schema 被零写入拒绝，提交后 Project 仍 OPEN。Desktop 0.10.15 已实测 HIGH 组接受后仍 `OPEN + v2`，最终精确确认后同一对象为 `COMPLETED + v3`、Proposal/Commit 为 `APPLIED`/`COMPLETED`；reload 后 Closure 全文可读、Focus 计数为 0 且 Now Work 不显示该 Project、原 Project 页可搜索打开。Pending/Recovery=0，SQLite integrity/FK 通过；没有新增表、状态、恢复器或写路径。证据见 `docs/runtime/V2_PROJECT_CLOSURE_EXTERNAL_AGENT_REPORT.md`。 |
| E2E-21 | V2 §68 | DeepSeek 认证、模型、中文、Schema | 模型输出非权威 | llm smoke | Provider | explicit live | D | DONE | `deepseek-v4-flash` 真实中文 Structured Output smoke 通过；Flash/Pro 的严格案例均真实到达机器 envelope/hash 与正式 Validator。Key 只经 Keychain reference 解析，认证、实际模型、中文和 Schema 正向范围已完成；不引用已作废的早期 22-run。 |
| E2E-22 | V2 §68 | Journal -> 真实 Proposal -> Diff | Validator 成功前零写入 | analyze/validate | Provider/Review | live + Desktop | D | DONE | Flash/Pro 严格无答案黄金套件各完成 22/22 `PIPELINE_PASS`。L4 Desktop 真实 Flash 又完成当前块入口、loading/error、READY 与 Diff、`NO_PROPOSAL`、直接接受、拒绝、同 proposal_id 调整后接受；确定性 external Agent fixture 验证两组部分接受且明确不冒充模型输出。Object/Commit 仍为 0，详见 L4 Runtime Report 与脱敏交互 JSON。 |
| E2E-23 | V2 §68 | timeout/cancel/rate/invalid JSON 隔离 | 失败零 Candidate/Proposal/正式写入 | cancel/error map | async Provider | mock + bounded live | D | DONE | Mock/回归覆盖非法 JSON、空内容、截断、401/404/429/503、网络、取消和 timeout，Provider 10/10 PASS、0 skipped；真实 endpoint 已取得截断、timeout、空 Structured Content，以及单次无效临时 token 的 `401→LLM_AUTH_FAILED`。真实 Keychain 配置请求开始后 10ms Abort，在 20ms 内收口 `LLM_CANCELLED`；限流按规范允许的模拟边界，用真实 loopback HTTP 429 验证生产 fetch、请求外形、两次有限尝试和 `LLM_RATE_LIMITED`，不向 DeepSeek 制造洪峰。探测只调用 Provider 层，Graph/Candidate/Proposal/正式 Store 写入均为 0，且不保存 Key、Authorization、response body 或 request id；证据见 `docs/runtime/V2_DEEPSEEK_E2E23_ERROR_CLASSIFICATION_REPORT.md`。 |
| E2E-24 | V2 §68 | 凭据安全 | secret 永不持久化或导出 | diagnostics/report | logs/Git/Graph scan | canary + live artifact scan | D,F | DONE | 真实 Key 只经 Keychain reference 使用；Git 工作区与 ignored Graph canary 保持无匹配。L4 Desktop 默认日志、两份实际 Diagnostics JSONL、机器可读交互结果和 Runtime Report 均完成脱敏扫描；不持久化原始 response header、Provider request id 或未经 Validator 的失败正文。 |

## 完成更新规则

每项升级为 `DONE` 前必须同时补：代码路径、自动测试名、失败路径、Desktop/真实 API 证据（适用时）、文档和已知限制。Slice Gate 与 Release Gate 只从本矩阵聚合，不从文件存在或单次成功推断。
