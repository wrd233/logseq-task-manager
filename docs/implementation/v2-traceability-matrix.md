# V2 设计到代码追踪矩阵

> 初版日期：2026-07-19。状态只表示 V2；V1 自动测试只能登记为 `REUSE_EVIDENCE`，不能把 V2 条目标成完成。

## 状态

- `NOT_STARTED`：V2 尚无实现；
- `REUSE_EVIDENCE`：V1 有可复用代码/测试，但 V2 契约或架构尚未满足；
- `AUTOMATED_FOUNDATION`：V2 代码、自动成功/失败路径和文档已建立，但 Desktop 或真实进程 Gate 尚未完成；
- `DESKTOP_PARTIAL_PASS`：已有真实 Desktop 与持久化证据，但该需求的其他交互、失败路径或视觉 Gate 仍未全部完成；
- `RUNTIME_BLOCKED_BY_CONFIG`：自动骨架可继续，最终 Gate 依赖用户提供真实配置；
- `DONE`：代码、失败路径、文档和对应 Gate 证据全部通过。

## 横切要求

| requirement_id | 文档章节 / 决定 | 用户场景 | Domain 规则 / 不变量 | Application 用例 | Adapter / UI | 测试 | Slice | 状态 | 当前证据 / 缺口 |
|---|---|---|---|---|---|---|---|---|---|
| V2-ARCH-001 | D-180..191；V2 §5.4-5.5 | Plugin、CLI、Agent 看到同一事实 | 正文/状态/历史/投影单一权威 | 所有 command/query 共享一层 | Service client；SQLite adapter | boundary + integration | A | REUSE_EVIDENCE | V2 Application/Repository 原子 command seam 与 CLI 已通过；显式 Block 的 Plugin 事件统一经 Local Service 写 SQLite，断线队列仅在会话内保存意图且不构成第二权威；V2 descriptor 路径不初始化 V1 FileStorage/Application 写入，其他 V2 write routes 仍关闭 |
| V2-ARCH-002 | D-216；V2 §30-32 | Service 不可用仍可写正文 | Domain 无 transport 语义 | health/status/restricted | 单个 loopback Service | unavailable/protocol | A | REUSE_EVIDENCE | descriptor 0600、auth、timeout/unavailable/protocol restricted 自动测试通过；Plugin 已实现 Electron 安全读取、probe、脱敏受限态与无 Store 首次启用分支，待 Desktop 验收 |
| V2-DATA-001 | D-190..192；V2 §33-34 | 每 Graph 独立运行和恢复 | SQLite 唯一当前状态；schema 独立版本 | initialize/migrate/backup | SQLite + `.task-copilot/` | idempotency/locked/corrupt | A | AUTOMATED_FOUNDATION | Graph-bound schema v10；显式快照后 v1..v9→v10 受控升级，v3 Commit ledger、v4 Proposal 表、v5 Audit/current projection 解耦、v6 nullable Task due_at、v7 有界 V1 migration ledger、v8 nullable Project closure_json、v9 单一普通 Association 与 v10 Candidate review state 均有迁移记录；失败全量回滚/重试、事务幂等、corrupt/unknown、写锁零写入、Doctor、Backup 和 Restore 原语已自动测试。v10 Candidate 不存正文，仅保存设计既定审阅元数据；Desktop 专用库尚未升级。 |
| V2-DOM-001 | D-214、D-220；V2 §13-22 | 六类对象、三层状态可理解 | 六类封顶；Lifecycle/Condition/Focus 封顶 | create/change/focus | UI 弱提示 | domain matrix | A | REUSE_EVIDENCE | V2 pure Domain contracts 与 Application create/lifecycle/change_condition tests 通过；Condition 现有 JSON 字段经 Service 封闭输入，WAITING/PAUSED 复查时间强校验，版本/幂等/Audit 单事务且不改变 Lifecycle/Focus。对象特定完整语义/UI 待后续 Slice |
| V2-ID-001 | D-030..041；V2 §21 | 移动/改名不丢身份 | object_id 独立；最多一 Primary Anchor | bind/observe/rebind | Graph UUID adapter | uniqueness/move/delete | A-B | DESKTOP_PASS | 初始绑定、观察、move/copy 与 rebind 合同已通过；Desktop 原生跨页移动在 reload 后保持同 object_id/active anchor_id/Block UUID/版本/Ownership/Association，去 properties 复制得到独立 UUID/object_id/anchor_id。rebind 会在确认和 stale 重读后持久化并复核 `id:: <UUID>`，再以新 Logseq version 提交；旧 Anchor 保留 replaced，新 active Anchor/Object/Audit/Receipt 单事务且不改 object_id/Ownership。cold reload 延迟首轮已知 Anchor reconciliation，避免索引窗口假 missing，未新增扫描器或恢复器；删除 Anchor 的完整审阅路径仍由 E2E-06 继续验收。 |
| V2-OWN-001 | D-035..047；V2 §21 | 位置变化不改归属 | 最多一个 Primary Owner；Association 不冒充归属 | change ownership/add association | 对象页显式影响与确认 | matrix/cycle/scope | A-C | DESKTOP_PARTIAL_PASS | Primary Ownership 继续由唯一表/事务维护，并由只读 Service 投影在对象页以 `child → owner / 唯一主归属` 显示，与普通 Association 明确分开。显式 change 只接受唯一 HIGH `CHANGE_OWNERSHIP` Proposal，Service 权威重读对象，以一个 DOMAIN_WRITE SemanticCommit step 调用同时保护 child/new Owner/current Owner 的原子 Application command；Plugin 有专用确认、stale/错误/成功、busy 和重复提交保护。专用 inverse Commit 交叉校验审阅前 Owner 与正向 receipt，原子恢复旧 Owner 或未归属；后续 child/Ownership 变化零写入，receipt 后中断可重启幂等续完并标记正向 UNDONE。未完成正向 Commit 锁住 review/revalidate；故障注入覆盖正反向 receipt 后续完、prepare 后 Owner 版本漂移/删除、非法类型矩阵及 FAILED ledger 到 Proposal 终态之间的重启补齐。普通 Association Desktop 已通过独立确认、SQLite/Audit 读回与 reload；Primary Ownership Desktop 已通过 external Proposal-only、HIGH 接受零正式写、版本重验、Commit、reload、Undo 与再次 reload，Task v5→v6→v7，Project v2、Anchor 和 Association 不变。首次运行发现 Project page Anchor 被 Block reconciliation 误判 missing；版本保护安全停写后，复用对象类型投影限定 Block workflow 并以 TDD/真实重跑闭环。仍缺真实跨页移动不改归属 Gate；证据见 `docs/runtime/V2_ASSOCIATION_DESKTOP_REPORT.md` 与 `docs/runtime/V2_OWNERSHIP_DESKTOP_REPORT.md`。 |
| V2-PROP-001 | D-094..122；V2 §23-28 | 可读、可拆分、可过期的建议 | Proposal 非事实；read/modify scope；group dependency | validate/revise/review/revalidate | 两文件、文本/语义 Diff | schema/stale/partial | C | AUTOMATED_FOUNDATION | V2 runtime Schema、两文件、scope/hash/risk/dependency、schema v4 持久化、submit/list/get/review 和 Review UI 已通过。C3 重读 read scope 与 accepted modify target；最终确认仍在同一卡片，接受、检查通过和正式生效有明确区分。C4/C5 自动闭环已接入，缺 Desktop Gate |
| V2-COMMIT-001 | D-185..188；V2 §29 | 提交失败不假成功，可 Undo | version/hash；Partial Failure；Undo 不覆盖新编辑 | commit/undo/recover | Graph + SQLite step ledger | fault injection/restart | C | AUTOMATED_FOUNDATION | C4/C5 已形成 PENDING-first Commit、Graph before/after 重读、Application Object/Anchor/Audit/Receipt 物化、inverse Commit、精确当前投影删除、正向 `UNDONE`、幂等重试和 PENDING/RECOVERY_REQUIRED 重启续跑。Review UI 显示最终确认、已生效与 Undo；Graph/Object/Anchor/Ownership/Focus/额外 Anchor 任一后续变化均停写。缺真实 Desktop 连续闭环和进程中断 Gate |
| V2-LLM-001 | D-125..144；V2 §40-46 | 局部语义产生 Proposal | Provider/Model 不进 Domain；非法输出零写入 | analyze/scan/revise | Provider/Prompt/Profile runtime | mock/live/golden/security | D | RUNTIME_BLOCKED_BY_CONFIG | L1 自动基础已完成：显式 runtime 配置与 env/Keychain secret reference、请求/Structured Output、timeout/cancel、有限 retry、认证/限流/5xx/network/空/非法/截断分类、五层 Prompt、机器来源与 hash、Domain Validator、`NO_PROPOSAL` 零写入、Local Service 审阅队列及 Plugin 当前块入口均通过。L2 单请求 harness 默认不开网，DS-01..12 manifest 已固定。先前真实请求只证明可达且以 `finish_reason=length` 失败；仍缺当前安全配置下 L2/L3/L4 真实 Gate 与 Desktop。 |
| V2-VIEW-001 | D-145..171；V2 §9-10、§57-59 | Now Work 与审阅中心低噪可扫读 | Projection 可重建；无 AI 分数 | query now/review | 三区域；两视图；键盘/主题 | VM/UI/Desktop | E | DESKTOP_PARTIAL_PASS | Now Work Desktop 证据保持。Candidate 已具备跨刷新 SQLite 统一列表、到期再现、保持普通、同来源抑制、生成唯一 Proposal、Commit resolved 与 Undo reopen 闭环；扫描只写 Candidate。CREATE 和 UPDATE Candidate 的原文优先、Diff、accepted-not-applied、Commit、Undo、reload 与 stale 零写入均已通过 Desktop。本项仍缺整个 Candidate 列表、Project/Area 筛选、键盘与深浅主题 Desktop Gate，因此保持部分通过。 |
| V2-CLI-001 | D-128..135；V2 §36-39 | Agent 确定性获取有限上下文 | CLI 无领域逻辑、无 force apply | object/graph/context/proposal/skill/doctor | Service API + CLI | help/json/exit/integration | A,F | AUTOMATED_FOUNDATION | 可执行 status/doctor/object/Proposal list/show/validate/submit、Skill list/show 与 Context object/project export、JSON schema、退出码和真实 Service 集成通过。`object list` 复用 Service 投影并支持六类对象/四种 Lifecycle 筛选，`object search` 只在同一只读投影检索 object_id/正文；非法筛选在请求对象投影前拒绝，没有新协议、索引或状态源。Context 是有界只读派生包，已真实导出包含两份嵌套 Skill 的 Project 包；CLI 对每个 component 做有界路径校验，仍拒绝空段/穿越/绝对路径并校验 hash/bytes。validate 零持久化，submit 只进入共享审阅队列且没有 apply/commit。Graph 缺口仍明确；graph/block/page 读取尚未实现。 |
| V2-MIG-001 | D-193..202；V2 §51 | 用户小批次迁移且可撤销 | 手动、幂等、部分采用合法 | scan/preview/commit/undo batch | Settings/CLI | interruption/repeat/rollback | F | AUTOMATED_FOUNDATION | 全部旧 Phase/Condition/Signal 的确定性可审阅映射与校验 Recovery Bundle Scan 已通过；Application/SQLite 支持 ≤50 项原子 Object/Anchor/Ownership 导入、同内容幂等、Verify、变化保护 Undo、Undo 后重试、重启续读和单一 Activate。Local Service 只接受服务端 Backup ID 并只读校验同 Graph/schema/integrity；CLI 完整暴露 preview/show/import/verify/undo/activate，三项写切换均需精确确认。真实 CLI→Service 集成在 IMPORTING 后关闭/重启 Service，成功读回并继续 Verify/Activate；Plugin 迁移工作区仅读投影同一 SQLite 账本的 Run/计数/恢复点/下一步，不接收 Bundle 或执行直接写入。迁移账本不复制当前正文且不复用 SemanticCommit。尚缺 Desktop copied-data 和 Plugin 视图真实验收。 |
| V2-OPS-001 | D-192、D-203..204；V2 §53-56 | 故障可诊断、备份可恢复、Key 安全 | 高影响修复走 Proposal；secret 永不记录 | backup/restore/doctor/diagnostics | Settings/CLI | restore/redaction | A,F | AUTOMATED_FOUNDATION | 受控 Service Backup API、0700/0600、防覆盖、Graph/schema/integrity/foreign-key 只读校验、路径隔离和错误脱敏已自动证明。结构化 Doctor 已覆盖 Anchor、Identity、Stale Proposal、Pending/Recovery Commit、最新 Backup、Key/Provider、Skills 和协议，FAIL 报告可由 CLI 读取并返回退出码 7；0600 脱敏 diagnostics.zip 带 SHA-256 清单且真实 `unzip -t` PASS。Graph/Desktop 观察、Provider live health 与 Service 日志采集仍待专用 Gate。 |
| V2-FIRST-001 | D-197..198；V2 §32.1、§33 | 首次启用时空系统可理解、可选择下一步 | 首次启动不扫描、不迁移、不调用模型 | initialize/check graph/status | 非敏感配置模板；欢迎页仅含开始使用、迁移现有内容、检查系统状态；失败进入受限模式 | first-run/reload/Desktop | A | REUSE_EVIDENCE | 自动测试与 2026-07-20 Desktop 均证明三入口、空 descriptor 在 Adapter/V1 领域 FileStorage 前停止、Store NOT_STARTED、formal writes false、普通正文可编辑；零 Provider/迁移成立。仍需协议错误等 A-RT-03 剩余项后才能 DONE |

## E2E-01..24

| requirement_id | 文档章节 | 用户场景 | Domain 规则 / 不变量 | Application 用例 | Adapter / UI | 自动 / 运行测试 | Slice | 状态 | 当前证据 / 缺口 |
|---|---|---|---|---|---|---|---|---|---|
| E2E-01 | V2 §68 | 写 `[任务]`，重启、改标题、Now Work 可见 | Task OPEN；ID/Anchor 稳定 | materialize/sync/query | block event/parser/Now | fixture + Desktop | B,E | DONE | `explicit-sync-controller.ts` 与 `v2-explicit-candidate-discovery.ts` 复用同一 Service/Proposal 边界；自动测试 `service recovery reconciles only known Anchors and reports missing or removed markers`、`manual candidate discovery traverses only the current page within explicit bounds`、`candidate persistence rereads the whole preview before sending bounded Candidate-only commands` 覆盖恢复、有界扫描与 stale 零写入。Desktop 已证明真实物化、同 object_id/anchor_id 标题更新、Now Work/reload，以及 Plugin 关闭时建立四个显式 Block、重启后当前页扫描只保存 Candidate、单项正式化 Commit/Undo/reload；证据见 `docs/runtime/V2_CANDIDATE_REVIEW_DESKTOP_REPORT.md`。当前页多项逐项失败和 stale UI 仍在专用 Gate 中，但不改变本场景已通过。 |
| E2E-02 | V2 §68 | `[任务] 标题` 无 Marker | Marker 不决定身份 | materialize | parser | unit + Desktop | B | REUSE_EVIDENCE | 有/无 Marker Parser、Plugin 事件交付与无 Marker 首次物化路径已自动证明；Marker 自动合同证明 TODO/NOW/DOING/WAITING 不决定身份、Condition 或 Focus，DONE 对简单 Task 请求完成，CANCELED 在记录原因前零写入。Desktop Marker 未完成 |
| E2E-03 | V2 §68 | Task 下裸 TODO 保持内部步骤 | 内部 TODO 无 object_id | synchronize block | subtree parser | fixture + Desktop | B | REUSE_EVIDENCE | B0 Parser 与 B1 有限子树 fixture 已证明：事件根 UUID 经 300ms 防抖、32 项覆盖队列与 256 Blocks frontier 预算后重读权威正文；Task 下裸 TODO 为 `NONE` 且零 Service 写入，嵌套显式 Decision 独立同步；队列溢出/截断/异常只提交已验证前缀并对其余项要求 reconciliation，根不可读零写入，注销停止后续读取和交付。真实 Desktop 事件 shape、粘贴与快速编辑未完成 |
| E2E-04 | V2 §68 | 跨页移动 | ID/Ownership 不变，Anchor 更新 | observe/sync | UUID event | integration + Desktop | B | DONE | 自动 fixture 与 Desktop 0.10.15 均通过：Logseq 原生 Cut/Paste 跨页移动后，同 UUID 经统一 Service 保持 object_id/active anchor_id、版本、Primary Ownership 和普通 Association；冷 reload 后读回一致，源页不再含该 Block。 |
| E2E-05 | V2 §68 | 复制正式 Block | 新 UUID 不继承 ID | detect copy/candidate | Graph adapter | fixture + Desktop | B | DONE | 自动 fixture 与 Desktop 0.10.15 均通过：去 properties 复制正式显式 Task 得到新 Block UUID、新 object_id 与新 active anchor_id；原对象保持不变，复制对象不继承 Ownership/Association，冷 reload 后两者均 active。真实事件证明 `event.blocks` 已足够，未保留会误收瞬态空 Block 的 `txData` 平行补漏。 |
| E2E-06 | V2 §68 | 删除 Primary Anchor | 对象保留、Anchor Conflict | consistency check/rebind | Graph adapter/review | fake + Desktop | B,E | DESKTOP_PARTIAL_PASS | 已知 UUID 缺失/冲突观察保留 Object；同 UUID可恢复 active。Desktop 已完成 missing→显式 rebind→reload active：确认后先持久化并复核新 Block `id:: <UUID>`，旧 Anchor 原子保留 replaced，新 active Anchor 沿用 object_id；5 秒 cold-index 窗口后 reconciliation 无假 missing。完整“删除 Anchor→审阅→rebind”连贯 UI 场景仍待独立验收。 |
| E2E-07 | V2 §68 | 分析普通 Block | 模型只建议 | analyze block | LLM/Review | mock + live + Desktop | D | RUNTIME_BLOCKED_BY_CONFIG | Review Center 已按 capability 显示当前选中 Block 分析，普通记录可返回 `NO_PROPOSAL` 理由且零持久化；有效输出只进入 READY 审阅队列，正式对象计数保持 0。真实 DeepSeek 与 Desktop 待验收。 |
| E2E-08 | V2 §68 | 接受表达、拒绝升级 | group dependency 合法 | review/commit/revalidate | Review Center | property + Desktop | C | DESKTOP_PARTIAL_PASS | 自动 group dependency、部分接受、高影响确认、拒绝升级、accepted-only Commit 仍通过。Desktop 已真实显示上下文/最终预览/文本与语义 Diff，完成 MEDIUM 组接受且明确未生效、提交前重验、独立最终确认和正式 Commit；高影响、拒绝/暂缓与多组依赖 Desktop 尚缺。 |
| E2E-09 | V2 §68 | 原文变化后提交旧 Proposal | UUID/hash/version 不匹配停写 | validate/commit | Graph + SQLite | conflict integration | C | DONE | V2 C3/C4 在 prepare 与 Graph 写前后重读 UUID/hash/version；真实 Desktop 接受 Proposal 后修改原文，再确认最终 Commit，Proposal 变为 `STALE`，原文保持后续内容，对应 Object/Anchor/Commit 均为 0；没有静默覆盖。 |
| E2E-10 | V2 §68 | Undo 正文和 Store，不覆盖后续编辑 | Undo 新 Commit | undo/recover | Graph + SQLite | fault + Desktop | C | DESKTOP_PARTIAL_PASS | 自动 inverse Commit、重启续跑、后续编辑零覆盖仍通过。Desktop 已真实 Commit 后修改正文，再确认 Undo；UI 明示“对象或 Anchor 已有后续变化；Undo 没有写入”，确认框关闭，后续正文/Object 保留，正向 Commit 仍 `COMPLETED`、没有逆向 Commit。正常 Undo 与 cold reload 已通过；真实 Commit/Undo 进程故障尚缺。 |
| E2E-11 | V2 §68 | Focus/期限/阻碍/Waiting 可解释排序 | Focus 非状态；Waiting 安静；无分数 | query now work | Now Work UI | VM + Desktop | E | DONE | 自动成功/失败合同保留。Desktop 已真实点击 Focus 加入、两项上移排序、WAITING/BLOCKED 表单、Task 本地时间期限、可读 blocker 选择、阻碍对象唤醒、Task 筛选/按类型分组和 Primary Anchor 打开；CLI 读回对象 version 9 及 Condition/dueAt/blockerObjectId，冷重启后 Focus 顺序/期限/状态保持，无分数。future-review Waiting 不再误报到期，已在 Focus 中的等待项不再显示重复加入操作。证据见 `docs/runtime/V2_SLICE_A_B_DESKTOP_REPORT.md` |
| E2E-12 | V2 §68 | 审阅中心原文优先、四处置 | Candidate/Proposal 分离 | disposition/review | two-tab Review | UI + Desktop | E | DONE | SQLite v10 持久化单一 Candidate authority，正文只在 Logseq；Plugin 对显示队列和来源瞬态重读使用同一个 50 项上限，并按原文→理由→建议呈现。同版本记忆处置，普通拒绝随来源变化重开，`NO_MORE_LIKE_THIS` 仅在稳定来源+类别+建议相同且正文普通编辑时保持，不会压制不同目标建议；已有当前 Proposal 时以 409 拒绝平行处置。Service 集成与 Desktop 均已真实走完 CREATE 扫描、四处置、唯一 Proposal、READY/ACCEPTED 零正式写、Commit resolved、Undo reopen 与 reload。UPDATE 也已在 Desktop 走完目标选择、编辑完整最终正文、红/绿文本 Diff、单组 `REWRITE_BLOCK`、accepted-not-applied、同 object_id 版本 4→5 Commit、reload、逆向 Undo 版本 5→6、再 reload 与 Candidate reopen。真实运行发现并修复 Logseq `id::` 持久化的 identity-only 假 stale：复用 Candidate rediscovery 仅刷新未改正文版本，正文 hash 变化仍零写入拒绝。Pending/Recovery Commit=0、SQLite integrity/FK 通过，无新表、状态、Commit 类型或恢复器；合同见 `docs/implementation/V2_CANDIDATE_UPDATE_CONTRACT.md`，Desktop 证据见 `docs/runtime/V2_CANDIDATE_REVIEW_DESKTOP_REPORT.md`。 |
| E2E-13 | V2 §68 | 外部 Agent 导出、验证、提交、插件审阅 | submit != commit；scope 封闭 | export/validate/submit | CLI/Review | CLI integration + Desktop | F | AUTOMATED_FOUNDATION | object/project Context Package 已从真实 Service 导出只读事实、范围/版本/hash 与完整 Skills，导出零正式写入；固定外部 Proposal 又经 CLI → Service validate/submit，validate 后 Proposal=0，submit 后 READY Proposal=1，而 Object=0、SemanticCommit=0。CLI 不提供 apply/commit；尚缺 block/page Graph context 与 Plugin Desktop 跨入口审阅 Gate。 |
| E2E-14 | V2 §68 | 手动小范围迁移、预览、幂等、Undo | migration batch 可恢复 | scan/commit/undo | Settings/CLI | fixture + Desktop | F | AUTOMATED_FOUNDATION | 校验 Recovery Bundle 的 Scan 与逐项审阅基础保留；真实 CLI→Service 流程已创建并校验同 Graph 快照，完成 Preview、≤50 项 Import，在 IMPORTING 后关闭/重启 Service，再 Show、Verify 与 Activate；Service 集成另证明 Undo、重试，Persistence fixture 证明变化保护、约束/锁零半写。Plugin 已能在不引入可编辑副本或直接写路径的前提下查看迁移进度；尚缺 Desktop copied-data 与该视图真实交互 Gate。 |
| E2E-15 | V2 §68 | Service 故障时正文可编辑 | 无 Service 不允许正式写入 | health/recover/check | Plugin restricted | process fault + Desktop | A | REUSE_EVIDENCE | 自动合同保留；2026-07-20 Desktop 已停止真实 Service、连续保存两次正文、观察 deferred issue，并在不 reload Plugin 的情况下更新 descriptor key 重连；SQLite 同一对象只收到 latest 值，version 3→4。Diagnostics 已显示显式队列状态；reload 受限态、协议错误及溢出仍待验收 |
| E2E-16 | V2 §68 | Graph 成功、Store 失败 | 明确 Partial Failure | commit/recover | Graph + SQLite | injected failure | C | REUSE_EVIDENCE | V1 Domain save failure/compensation 已测 |
| E2E-17 | V2 §68 | 恢复快照后 Doctor PASS | 恢复后必须一致性检查 | backup/restore/doctor | SQLite/CLI | temp restore + Desktop | A,F | REUSE_EVIDENCE | Backup 校验、离线 Restore 恢复点/原子激活/Doctor/失败回滚已通过；Service Apply 固定确认、关闭 live Store、descriptor 删除已通过。真实 CLI create→restore→stop→restart→Doctor 进程冒烟 PASS；Desktop 待完成 |
| E2E-18 | V2 §68 | Key 不出现在任何默认资产 | secret 非领域数据、永不记录 | config/log/export | Provider/diagnostics | secret canary scan | D,F | AUTOMATED_FOUNDATION | Provider 只接受 env/Keychain reference，响应正文、Authorization 与凭据不进入结构化错误或 Plugin 日志；Mock canary 已通过。仍缺真实运行产物、Diagnostics、Graph 与 Git 的 live canary 扫描。 |
| E2E-19 | V2 §68 | Project 对象和页面原子创建 | Project 必须页面；不半成功 | prepare/finalize project | Projects UI + exact Page Adapter + SQLite ledger | integration/fault injection + Desktop | B | DONE | 自动合同覆盖 prepare/finalize、未知同名/意图不匹配零写入、幂等、改名和事务 fault injection。Desktop 已真实创建 Project 并在 V2-only 对象列表显示 Lifecycle/Condition；未知同名页 UUID/属性不变且零对象新增；受控页改名与冷 reload 保持 UUID/object_id。真实拦截 finalize 并停止 Service 后 descriptor 清理、SQLite 目标对象为 0、受控页保留；重启同意图复用预发行身份并只收口一个 Project。复用现有 SemanticCommit step ledger，无专用表/双写/扫描；证据见 Desktop 报告。 |
| E2E-20 | V2 §68 | Closure 可说明未完成目标 | Completion 不要求全部 Objective | close project Proposal | Agent/Review | fixture + Desktop | F | DONE | 外部 Agent Proposal 用一个 HIGH 组绑定结构化 Closure 与 COMPLETED；Validator 强制原始目标/结果/Deliverable/未完成原因与后续/遗留/Decision/未来总结，但不强迫所有 Objective 完成。固定 fixture 与真实独立 Agent 输出均经真实 CLI 文件 `validate → submit → Local Service`进入 Review queue；首次 Agent 错误 Schema 被零写入拒绝，提交后 Project 仍 OPEN。Desktop 0.10.15 已实测 HIGH 组接受后仍 `OPEN + v2`，最终精确确认后同一对象为 `COMPLETED + v3`、Proposal/Commit 为 `APPLIED`/`COMPLETED`；reload 后 Closure 全文可读、Focus 计数为 0 且 Now Work 不显示该 Project、原 Project 页可搜索打开。Pending/Recovery=0，SQLite integrity/FK 通过；没有新增表、状态、恢复器或写路径。证据见 `docs/runtime/V2_PROJECT_CLOSURE_EXTERNAL_AGENT_REPORT.md`。 |
| E2E-21 | V2 §68 | DeepSeek 认证、模型、中文、Schema | 模型输出非权威 | llm smoke | Provider | explicit live | D | RUNTIME_BLOCKED_BY_CONFIG | `/models` HTTP 200，`deepseek-v4-flash` 中文请求 HTTP 200；但 `finish_reason=length` 且输出非 JSON，Provider/Schema Gate 未通过 |
| E2E-22 | V2 §68 | Journal -> 真实 Proposal -> Diff | Validator 成功前零写入 | analyze/validate | Provider/Review | live + Desktop | D | RUNTIME_BLOCKED_BY_CONFIG | 自动纵向管道已到 Review Center：当前 Block → 五层 Prompt → Structured Output → 机器 envelope/hash → Validator → READY Proposal/Diff；模型伪造的 ID/source/APPLIED 被覆盖，非法 scope 零 Proposal。真实 DeepSeek 与 Desktop 待验收。 |
| E2E-23 | V2 §68 | timeout/cancel/rate/invalid JSON 隔离 | 失败零 Candidate/Proposal/正式写入 | cancel/error map | async Provider | mock + bounded live | D | AUTOMATED_FOUNDATION | Mock 已覆盖 timeout、外部 cancel、429、401/403、404、5xx、network、空内容、HTTP/内容非法 JSON、字段缺失、`finish_reason=length`、响应上限与 retry cap；错误不写 Proposal/对象。真实 endpoint 的安全错误分类仍按 L2 Gate 保留。 |
| E2E-24 | V2 §68 | 凭据安全 | secret 永不持久化或导出 | diagnostics/report | logs/Git/Graph scan | canary + live artifact scan | D,F | RUNTIME_BLOCKED_BY_CONFIG | secret reference、错误/响应脱敏与 Plugin 元数据日志自动合同已通过；仍需真实配置后的默认日志、Diagnostics、Graph、报告与 Git 工作区 canary 扫描。 |

## 完成更新规则

每项升级为 `DONE` 前必须同时补：代码路径、自动测试名、失败路径、Desktop/真实 API 证据（适用时）、文档和已知限制。Slice Gate 与 Release Gate 只从本矩阵聚合，不从文件存在或单次成功推断。
